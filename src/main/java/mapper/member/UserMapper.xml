<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.UserMapper">

    <!--
        USER 테이블 컬럼:
        id, email, password, name, phone, email_verified,
        nickname, profileImg, role, created_at, is_deleted, provider, provider_id

        수정: tel → phone, profile_img → profileImg, deleted → is_deleted,
              UserRole enum → String, emailVerified 추가
    -->
    <resultMap id="userResultMap" type="dto.member.UserDTO">
        <id     property="id"            column="id"/>
        <result property="email"         column="email"/>
        <result property="password"      column="password"/>
        <result property="name"          column="name"/>
        <result property="phone"         column="phone"/>
        <result property="emailVerified" column="email_verified"/>
        <result property="nickname"      column="nickname"/>
        <result property="profile_image"    column="profile_image"/>
        <result property="role"          column="role"/>
        <result property="createdAt"     column="created_at"/>
        <result property="deleted"       column="is_deleted"/>
        <result property="provider"      column="provider"/>
        <result property="providerId"    column="provider_id"/>
    </resultMap>

    <!-- 로그인 (email + password) -->
    <select id="findByEmailAndPassword" parameterType="map" resultMap="userResultMap">
        SELECT id, email, password, name, phone, email_verified,
               nickname, profile_image, role, created_at, is_deleted, provider, provider_id
        FROM USER
        WHERE email    = #{email}
          AND password = #{password}
          AND is_deleted = 0
    </select>

    <!-- 이메일로 조회 -->
    <select id="findByEmail" parameterType="string" resultMap="userResultMap">
        SELECT id, email, password, name, phone, email_verified,
               nickname, profile_image, role, created_at, is_deleted, provider, provider_id
        FROM USER
        WHERE email = #{email}
          AND is_deleted = 0
    </select>

    <!-- 이메일 중복 체크 -->
    <select id="isEmailExists" parameterType="string" resultType="int">
        SELECT COUNT(*) FROM USER WHERE email = #{email}
    </select>

    <!-- 전체 조회 (관리자용) -->
    <select id="findAll" resultMap="userResultMap">
        SELECT id, email, password, name, phone, email_verified,
               nickname, profile_image, role, created_at, is_deleted, provider, provider_id
        FROM USER
        WHERE is_deleted = 0
        ORDER BY id DESC
    </select>

    <!-- 회원가입 INSERT -->
    <insert id="insert" parameterType="dto.member.UserDTO">
        INSERT INTO USER (
            email, password, name, phone, email_verified,
            nickname, profile_image, role,
            created_at, is_deleted, provider, provider_id
        ) VALUES (
            #{email}, #{password}, #{name}, #{phone}, #{emailVerified},
            #{nickname}, #{profile_image}, #{role},
            NOW(), 0, #{provider}, #{providerId}
        )
    </insert>

    <!-- 소셜 회원가입 (카카오/네이버) -->
    <insert id="insertSocial" parameterType="dto.member.UserDTO">
        INSERT INTO USER (
            email, nickname, email_verified, role,
            provider, provider_id, created_at, is_deleted
        ) VALUES (
            #{email}, #{nickname}, 1, 'MEMBER',
            #{provider}, #{providerId}, NOW(), 0
        )
    </insert>

    <!-- 회원 정보 수정 -->
    <update id="update" parameterType="dto.member.UserDTO">
        UPDATE USER SET
            name       = #{name},
            phone      = #{phone},
            nickname   = #{nickname},
            profile_image = #{profile_image}
        WHERE id = #{id}
    </update>

    <!-- 비밀번호 변경 -->
    <update id="updatePassword" parameterType="map">
        UPDATE USER SET password = #{password} WHERE email = #{email}
    </update>

    <!-- 회원 탈퇴 (soft delete) -->
    <update id="delete" parameterType="int">
        UPDATE USER SET is_deleted = 1 WHERE id = #{id}
    </update>

</mapper>
