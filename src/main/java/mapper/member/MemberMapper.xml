<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.MemberMapper">

    <!--
        MEMBER 테이블 실제 컬럼:
        id, user_id, purpose, experience,
        height, weight, diet, exerciseCount_goal,
        address, latitude, longitude, goals

        ※ trainer_id, gym_id, next_session, lesson_count, last_session, STATUS, age
           → DB MEMBER 테이블에 없음. MEMBERSHIP_PT 테이블에서 JOIN 처리
    -->

    <resultMap id="memberResultMap" type="dto.member.MemberDTO">
        <id     property="id"                column="id"/>
        <result property="userId"            column="user_id"/>
        <result property="purpose"           column="purpose"/>
        <result property="experience"        column="experience"/>
        <result property="height"            column="height"/>
        <result property="weight"            column="weight"/>
        <result property="diet"              column="diet"/>
        <result property="exerciseCountGoal" column="exerciseCount_goal"/>
        <result property="address"           column="address"/>
        <result property="latitude"          column="latitude"/>
        <result property="longitude"         column="longitude"/>
        <result property="goals"             column="goals"/>
    </resultMap>

    <!-- USER JOIN 포함 resultMap (로그인 세션, 마이페이지용) -->
    <resultMap id="memberWithUserResultMap" type="dto.member.MemberDTO"
               extends="memberResultMap">
        <result property="name"          column="name"/>
        <result property="email"         column="email"/>
        <result property="nickname"      column="nickname"/>
        <result property="profile_image" column="profile_image"/>
        <result property="role"          column="role"/>
        <result property="emailVerified" column="email_verified"/>
        <result property="socialType"    column="provider"/>
        <result property="age"           column="age"/>
    </resultMap>

    <!-- MEMBERSHIP_PT JOIN 포함 resultMap (PT 정보가 필요한 화면용) -->
    <resultMap id="memberWithPtResultMap" type="dto.member.MemberDTO"
               extends="memberWithUserResultMap">
        <result property="trainerId"   column="trainer_id"/>
        <result property="gymId"       column="gym_id"/>
        <result property="nextSession" column="next_session"/>
        <result property="lessonCount" column="lesson_count"/>
        <result property="lastSession" column="last_session"/>
        <result property="status"      column="mp_status"/>
    </resultMap>

    <!-- MEMBER.id 로 조회 -->
    <select id="findById" parameterType="int" resultMap="memberResultMap">
        SELECT id, user_id,
               purpose, experience,
               height, weight, diet, exerciseCount_goal,
               address, latitude, longitude, goals
        FROM MEMBER
        WHERE id = #{id}
    </select>

    <!-- USER.id 로 MEMBER 조회 -->
    <select id="findByUserId" parameterType="int" resultMap="memberResultMap">
        SELECT id, user_id,
               purpose, experience,
               height, weight, diet, exerciseCount_goal,
               address, latitude, longitude, goals
        FROM MEMBER
        WHERE user_id = #{userId}
    </select>

    <!-- USER.email 로 MEMBER + USER JOIN 조회 (마이페이지, 세션 로그인용) -->
    <select id="findByEmail" parameterType="string" resultMap="memberWithUserResultMap">
        SELECT
            m.id, m.user_id,
            m.purpose, m.experience,
            m.height, m.weight, m.diet, m.exerciseCount_goal,
            m.address, m.latitude, m.longitude, m.goals,
            u.name, u.email, u.nickname, u.profile_image,
            u.role, u.email_verified, u.provider, u.age
        FROM MEMBER m
        JOIN USER u ON m.user_id = u.id
        WHERE u.email    = #{email}
          AND u.is_deleted = 0
    </select>

    <!-- USER.email 로 MEMBER.id 만 조회 -->
    <select id="findMemberIdByEmail" parameterType="string" resultType="int">
        SELECT m.id
        FROM MEMBER m
        JOIN USER u ON m.user_id = u.id
        WHERE u.email    = #{email}
          AND u.is_deleted = 0
    </select>

    <!-- 전체 조회 -->
    <select id="findAll" resultMap="memberWithUserResultMap">
        SELECT
            m.id, m.user_id,
            m.purpose, m.experience,
            m.height, m.weight, m.diet, m.exerciseCount_goal,
            m.address, m.latitude, m.longitude, m.goals,
            u.name, u.email, u.nickname, u.profile_image, u.role, u.age
        FROM MEMBER m
        JOIN USER u ON m.user_id = u.id
        WHERE u.is_deleted = 0
        ORDER BY m.id DESC
    </select>

    <!-- MEMBER INSERT (회원가입 step3 완료 시) -->
    <insert id="insertMember" parameterType="dto.member.MemberDTO">
        INSERT INTO MEMBER (
            user_id,
            purpose, experience,
            height, weight, diet, exerciseCount_goal,
            address, latitude, longitude, goals
        ) VALUES (
            #{userId},
            #{purpose}, #{experience},
            #{height}, #{weight}, #{diet}, #{exerciseCountGoal},
            #{address}, #{latitude}, #{longitude}, #{goals}
        )
    </insert>

    <!-- MEMBER 수정 (마이페이지, 운동계획 수정 통합) -->
    <update id="update" parameterType="dto.member.MemberDTO">
        UPDATE MEMBER SET
            purpose            = #{purpose},
            experience         = #{experience},
            height             = #{height},
            weight             = #{weight},
            diet               = #{diet},
            exerciseCount_goal = #{exerciseCountGoal},
            address            = #{address},
            latitude           = #{latitude},
            longitude          = #{longitude},
            goals              = #{goals}
        WHERE id = #{id}
    </update>

    <!-- 운동 계획만 부분 수정 (UpdatePlanController 용) -->
    <update id="updatePlan" parameterType="dto.member.MemberDTO">
        UPDATE MEMBER m
        JOIN USER u ON m.user_id = u.id
        SET
            m.goals              = #{goals},
            m.experience         = #{experience},
            m.height             = #{height},
            m.weight             = #{weight},
            m.diet               = #{diet},
            m.exerciseCount_goal = #{exerciseCountGoal}
        WHERE u.email    = #{email}
          AND u.is_deleted = 0
    </update>

    <!-- 프로필 이미지 업데이트 -->
    <update id="updateProfileImage" parameterType="dto.member.UserDTO">
        UPDATE USER SET profile_image = #{profileImage}
        WHERE id = #{id}
    </update>

    <!-- MEMBER 삭제 -->
    <delete id="delete" parameterType="int">
        DELETE FROM MEMBER WHERE id = #{id}
    </delete>

    <!-- trainer_id 로 회원 목록 (MEMBERSHIP_PT 조인) -->
    <select id="findByTrainerId" parameterType="int" resultMap="memberWithPtResultMap">
        SELECT
            m.id, m.user_id,
            m.purpose, m.experience,
            m.height, m.weight, m.diet, m.exerciseCount_goal,
            m.address, m.latitude, m.longitude, m.goals,
            u.name, u.email, u.nickname, u.profile_image, u.role, u.age,
            mp.trainer_id, mp.gym_id, mp.next_session,
            mp.lesson_count, mp.last_session,
            mp.status AS mp_status
        FROM MEMBER m
        JOIN USER u ON m.user_id = u.id
        LEFT JOIN MEMBERSHIP_PT mp ON mp.member_id = m.id
                                   AND mp.trainer_id = #{trainerId}
                                   AND mp.status = 'active'
        WHERE u.is_deleted = 0
          AND mp.trainer_id = #{trainerId}
        ORDER BY m.id DESC
    </select>

    <!-- gym_id 로 회원 목록 (MEMBERSHIP_PT 조인) -->
    <select id="findByGymId" parameterType="int" resultMap="memberWithPtResultMap">
        SELECT
            m.id, m.user_id,
            m.purpose, m.experience,
            m.height, m.weight, m.diet, m.exerciseCount_goal,
            m.address, m.latitude, m.longitude, m.goals,
            u.name, u.email, u.nickname, u.profile_image, u.role, u.age,
            mp.trainer_id, mp.gym_id, mp.next_session,
            mp.lesson_count, mp.last_session,
            mp.status AS mp_status
        FROM MEMBER m
        JOIN USER u ON m.user_id = u.id
        LEFT JOIN MEMBERSHIP_PT mp ON mp.member_id = m.id
                                   AND mp.gym_id = #{gymId}
                                   AND mp.status = 'active'
        WHERE u.is_deleted = 0
          AND mp.gym_id = #{gymId}
        ORDER BY m.id DESC
    </select>

    <!-- USER.id 로 MEMBER 조회 (단순 별칭 쿼리) -->
    <select id="selectMemberByUserId" parameterType="Integer" resultMap="memberResultMap">
        SELECT id, user_id,
               purpose, experience,
               height, weight, diet, exerciseCount_goal,
               address, latitude, longitude, goals
        FROM MEMBER
        WHERE user_id = #{userId}
    </select>

</mapper>
