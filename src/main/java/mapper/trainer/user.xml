<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">

    <resultMap id="userResultMap" type="user">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="tel" column="phone"/>
        <result property="nickname" column="nickname"/>
        <result property="profileImg" column="profile_image"/>
        <result property="role" column="role"/>
        <result property="createdAt" column="created_at"/>
        <result property="deleted" column="is_deleted"/>
        <result property="provider" column="provider"/>
        <result property="providerId" column="provider_id"/>
    </resultMap>

    <insert id="insertUserTrainer" parameterType="user"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user`
        <trim prefix="(" suffix=")" suffixOverrides=",">
            email,
            password,
            name,
            phone,
            nickname,
            profile_image,
            role,
            <if test="provider != null and provider != ''">
                provider,
            </if>
            <if test="providerId != null and providerId != ''">
                provider_id,
            </if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            #{email},
            #{password},
            #{name},
            #{tel},
            #{nickname},
            #{profileImg},
            #{role},
            <if test="provider != null and provider != ''">
                #{provider},
            </if>
            <if test="providerId != null and providerId != ''">
                #{providerId},
            </if>
        </trim>
    </insert>

    <select id="selectUserById" parameterType="int" resultMap="userResultMap">
        SELECT id, email, password, name, phone, nickname, profile_image, role, created_at, is_deleted, provider, provider_id
        FROM `user` WHERE id = #{id}
    </select>

    <update id="updateUser" parameterType="user">
        UPDATE `user` SET
            name     = #{name},
            email    = #{email},
            password = #{password},
            phone      = #{tel},
            nickname = #{nickname}
        WHERE id = #{id}
    </update>

    <update id="updateUserProfile" parameterType="user">
        UPDATE `user` SET
            name     = #{name},
            phone      = #{tel},
            nickname = #{nickname}
        WHERE id = #{id}
    </update>

    <select id="selectUserTrainer" parameterType="String" resultMap="userResultMap">
        SELECT
            id,
            email,
            password,
            name,
            phone,
            nickname,
            profile_image,
            role,
            created_at,
            is_deleted,
            provider,
            provider_id
        FROM `user`
        WHERE email = #{email}
          AND is_deleted = false
    </select>


</mapper>
