<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="mapper.gymTrainerView">
  	
  	
  	<select id="selectGymTrainerViewByGym" parameterType="int" resultType="dto.GymTrainerView">
  		SELECT
    		t.id,
   	 		u.name,
   	 		u.profileImg,
    		COUNT(tm.member_id) AS memberCount
		FROM TRAINER t
		JOIN USER u
			ON t.user_id = u.id
		LEFT JOIN TRAINER_MEMBER tm
    		ON t.id = tm.trainer_id
		WHERE t.gym_id = #{value}
		GROUP BY t.id, u.name, u.profileImg
  	</select>
  </mapper>